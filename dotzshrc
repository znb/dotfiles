ZSH=$HOME/.oh-my-zsh
source $ZSH/oh-my-zsh.sh

ZSH_THEME="agnoster"
DISABLE_UPDATE_PROMPT=true
DISABLE_AUTO_UPDATE=false
DISABLE_LS_COLORS=false
HIST_STAMPS="mm/dd/yyyy"
plugins=(git osx nmap history tmux brew)

# User configuration

export PATH="$PATH:/usr/local/bin:/usr/local/sbin:/usr/bin:/bin:/usr/sbin:/sbin:~/Scripts:${GOPATH//://bin:}/bin"
export VIMRUNTIME="/usr/share/vim/vim80"
export LSCOLORS=GxFxCxDxBxegedabagaced

# Aliases
alias la='ls -la'
alias gl="git log --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias gcm='git commit -m'
alias lrt='ls -lrt'
alias mv='mv -v'
alias rm='rm -vi'
alias srm='srm -vi'
alias cp='cp -v'
alias pis='ping -o ftp.is.co.za'
alias svim='sudo /usr/bin/vim'
alias skill='sudo kill -9'
alias tports='lsof -n -i tcp -P -sTCP:LISTEN'
alias uports='lsof -n -i udp -P'
alias dockerclean='docker ps -a | grep Exited | cut -d" " -f1 | xargs docker rm '
alias ofs='lsof | grep '

# Terraform 
alias tp='terraform plan'
alias ta='terraform apply'
alias tpd='terraform plan -destroy'
alias td='terraform destroy'
alias ti='terraform init'
